# Python dependencies for app.py. Versions are the ones verified with a local
# run on Python 3.13 (see README.md). The dev server manages its own venv and
# deploy-dev.sh does not install from this file.

Flask==3.1.3
Werkzeug==3.1.8
itsdangerous==2.2.0
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52
Flask-Migrate==4.1.0
Flask-Mail==0.10.0
Flask-Cors==6.0.5
Flask-SocketIO==5.6.1
requests==2.34.2

# Reports, exports and documents
pandas==2.3.3
openpyxl==3.1.5
reportlab==5.0.1
python-docx==1.2.0
pypandoc==1.17
pikepdf==10.13.0.post1

# Optional: attendance register PDFs. Imported only when one is generated, and
# on Windows it also needs the GTK runtime, so it is left out by default.
# weasyprint
